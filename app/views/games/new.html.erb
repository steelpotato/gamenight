<h1>New game</h1>

<% form_for(@game) do |f| %>
  <%= f.error_messages %>
  <div><%= f.text_field :title %></div>
  <div><%= f.text_area :title %></div>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', games_path %>
