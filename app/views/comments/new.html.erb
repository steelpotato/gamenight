<h1>New comment</h1>

<% form_for ([@game, @comment]) do |f| %>
  <%= f.error_messages %>
  <%= f.text_area :body %>
  <p><%= f.submit "Create" %></p>
<% end %>

<%= link_to 'Back', game_path(@game) %>
