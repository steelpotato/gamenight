<h1>New category</h1>

<% form_for(@category) do |f| %>
  <%= f.error_messages %>
  <% @f = f %>
  <%= render :partial => 'form' %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', categories_path %>
