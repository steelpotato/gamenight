

<% form_for(@game) do |f| %>
    <%= f.error_messages %>
    <div><%= f.text_field :title %></div>
    <div><%= f.text_area :description %></div>
    
    <h4>Categories</h4>
    <% for category in Category.find(:all) %>
        <div>
            <%= check_box_tag 'game[category_ids][]', category.id, @game.categories.include?(category) %>  
            <%= category.name %>
        </div>
    <% end %>
    <div></div>
    
  <p><%= yield %></p><!-- Adds the submit button -->
<% end %>
